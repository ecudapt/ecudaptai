# Core training stack (versions tested together)
transformers==4.41.2
peft==0.11.1
datasets==2.19.0
accelerate==0.32.1
bitsandbytes==0.43.1          # GPU quantization (QLoRA). On Windows, works on recent builds.

# Data processing
sentencepiece==0.2.0
protobuf==4.25.3

# Evaluation
rouge-score==0.1.2
sacrebleu==2.4.2

# Utilities
tqdm==4.66.4
tensorboard==2.17.0
# wandb==0.17.0               # Optional: uncomment if you’ll use W&B

# Text processing (from your main requirements)
beautifulsoup4==4.12.3
lxml==5.2.1

# ML for data filtering
scikit-learn==1.5.1

# Optional but recommended
einops==0.8.0
safetensors==0.4.3
# xformers==0.0.28.post1      # Optional but recommended on 2080S; see install note below
