# LLM Training Requirements
# Install with: pip install -r requirements_llm.txt

# Core training framework
transformers>=4.36.0
peft>=0.7.0
datasets>=2.16.0
accelerate>=0.25.0
bitsandbytes>=0.41.0

# PyTorch (choose based on your system)
torch>=2.1.0
# For CUDA 11.8: torch>=2.1.0+cu118
# For CUDA 12.1: torch>=2.1.0+cu121
# For CPU only: torch>=2.1.0+cpu

# Data processing
sentencepiece>=0.1.99
protobuf>=3.20.0

# Evaluation
rouge-score>=0.1.2
sacrebleu>=2.3.1

# Utilities
tqdm>=4.66.0
tensorboard>=2.15.0
wandb>=0.16.0  # Optional: for experiment tracking

# Text processing (from main requirements)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Machine learning for data filtering
scikit-learn>=1.3.0

# Optional but recommended
einops>=0.7.0
safetensors>=0.4.0
